# 博客系统前后端设计文档

## 一、项目概述

### 1.1 项目背景

本项目是一个个人博客系统，包含前台展示和后台管理两大模块。作者是一名 Go 开发者，希望通过这个博客分享 Golang 开发、AI 和 NAS 折腾经验。

### 1.2 项目目标

- **前台**：提供良好的阅读体验，支持文章浏览、分类筛选、标签过滤、评论互动
- **后台**：提供完善的内容管理能力，支持文章、分类、标签、评论、友链等管理
- **性能**：支持高并发访问，页面加载快速
- **可维护性**：代码结构清晰，易于扩展和维护

### 1.3 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **前端** | Vue 3 + Vite | 渐进式框架 + 极速构建工具 |
| **UI 框架** | Element Plus | 组件库（后台管理） |
| **样式** | Tailwind CSS | 原子化 CSS 框架 |
| **状态管理** | Pinia | Vue 3 官方状态管理 |
| **路由** | Vue Router 4 | 官方路由 |
| **HTTP** | Axios | 请求库 |
| **后端** | Go + Gin | 高性能 Web 框架 |
| **ORM** | GORM | Go 语言 ORM 库 |
| **数据库** | MySQL | 关系型数据库 |
| **缓存** | Redis | 热点数据缓存 |
| **认证** | JWT | 无状态认证 |
| **日志** | Zap + Lumberjack | 结构化日志 + 日志轮转 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端                                  │
├─────────────────────────────────────────────────────────────────┤
│   前台页面 (prototypes/web/)    │    后台管理 (prototypes/admin/)  │
│   - 首页/文章列表               │    - 仪表盘                       │
│   - 文章详情                    │    - 文章管理                     │
│   - 分类/标签筛选               │    - 分类/标签管理                 │
│   - 友链                        │    - 评论管理                     │
│   - 每日一问                    │    - 友链管理                     │
│   - 关于我                      │    - 媒体库                       │
│                                 │    - 系统设置                     │
└───────────────┬─────────────────┴───────────────┬───────────────┘
                │           HTTP/HTTPS             │
                ▼                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Nginx / 反向代理                           │
│              - 静态资源服务                                       │
│              - SSL 终止                                           │
│              - 负载均衡                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     后端服务 (Go + Gin)                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │   Router    │  │ Middleware  │  │   Controller │              │
│  │  路由分发   │→ │  中间件链   │→ │   业务处理   │              │
│  └─────────────┘  └─────────────┘  └──────┬──────┘              │
│                                           │                      │
│                                           ▼                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    Service 层                            │    │
│  │         业务逻辑处理、参数校验、事务管理                    │    │
│  └─────────────────────────┬───────────────────────────────┘    │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                  Repository 层                           │    │
│  │              数据库访问、ORM 操作                         │    │
│  └─────────────────────────┬───────────────────────────────┘    │
└────────────────────────────┼────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据存储层                                │
├─────────────────────────────┬───────────────────────────────────┤
│         MySQL               │           Redis                   │
│   - 文章、分类、标签         │      - 热点数据缓存                │
│   - 评论、友链              │      - Session 存储                │
│   - 用户、配置              │      - 计数器                      │
└─────────────────────────────┴───────────────────────────────────┘
```

### 2.2 后端分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        后端分层架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    HTTP Request                          │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Middleware（中间件层）                        │   │
│  │  - Recovery: Panic 恢复                                  │   │
│  │  - Logger: 请求日志                                      │   │
│  │  - CORS: 跨域处理                                        │   │
│  │  - Auth: JWT 认证                                        │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           Controller / API（控制器层）                    │   │
│  │  - 参数绑定与校验                                        │   │
│  │  - 调用 Service                                          │   │
│  │  - 返回统一响应                                          │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Service（业务逻辑层）                        │   │
│  │  - 业务规则校验                                          │   │
│  │  - 事务管理                                              │   │
│  │  - DTO 转换                                              │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            Repository（数据访问层）                       │   │
│  │  - 数据库 CRUD                                           │   │
│  │  - 查询条件构建                                          │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Database（数据库）                     │   │
│  │  - MySQL: 业务数据                                       │   │
│  │  - Redis: 缓存数据                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 依赖注入

项目采用**构造函数依赖注入**，依赖关系：

```
Controller 依赖 Service 接口
Service    依赖 Repository 接口
Repository 依赖 *gorm.DB
```

统一在 `internal/app/app.go` 的 `initDependencies()` 里装配：

```go
// Repository
userRepo := repository.NewUserRepository(a.mysqlDB)
articleRepo := repository.NewArticleRepository(a.mysqlDB)

// Service
userSvc := service.NewUserService(userRepo)
articleSvc := service.NewArticleService(articleRepo)

// Controller
userCtrl := api.NewUserController(userSvc)
articleCtrl := api.NewArticleController(articleSvc)
```

---

## 三、前端设计

### 3.1 项目结构

#### 前台项目（blog-web）

```
blog-web/
├── public/
│   └── index.html
├── src/
│   ├── api/                # API 请求
│   │   ├── article.js      # 文章相关
│   │   ├── category.js     # 分类相关
│   │   ├── tag.js          # 标签相关
│   │   ├── comment.js      # 评论相关
│   │   ├── link.js         # 友链相关
│   │   ├── daily.js        # 每日一问
│   │   └── index.js        # 统一导出
│   ├── assets/             # 静态资源
│   │   ├── styles/         # 样式文件
│   │   └── images/         # 图片资源
│   ├── components/         # 公共组件
│   │   ├── layout/         # 布局组件
│   │   │   ├── AppHeader.vue
│   │   │   ├── AppSidebar.vue
│   │   │   └── AppFooter.vue
│   │   ├── article/        # 文章相关组件
│   │   │   ├── ArticleCard.vue
│   │   │   └── ArticleList.vue
│   │   ├── comment/        # 评论组件
│   │   │   └── CommentList.vue
│   │   ├── daily/          # 每日一问组件
│   │   │   └── DailyQuestion.vue
│   │   └── common/         # 通用组件
│   │       ├── SearchModal.vue
│   │       └── BackToTop.vue
│   ├── composables/        # 组合式函数
│   │   ├── useTheme.js     # 主题切换
│   │   ├── useSearch.js    # 搜索功能
│   │   └── usePagination.js # 分页
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── stores/             # Pinia 状态管理
│   │   ├── article.js
│   │   └── app.js
│   ├── views/              # 页面视图
│   │   ├── Home.vue        # 首页
│   │   ├── Archives.vue    # 文章归档
│   │   ├── Article.vue     # 文章详情
│   │   ├── Links.vue       # 友链
│   │   ├── Media.vue       # 娱乐
│   │   └── About.vue       # 关于我
│   ├── App.vue
│   └── main.js
├── index.html
├── package.json
├── vite.config.js
└── tailwind.config.js
```

#### 后台项目（blog-admin）

```
blog-admin/
├── public/
│   └── index.html
├── src/
│   ├── api/                # API 请求
│   │   ├── article.js
│   │   ├── category.js
│   │   ├── tag.js
│   │   ├── comment.js
│   │   ├── link.js
│   │   ├── daily.js
│   │   ├── media.js
│   │   ├── settings.js
│   │   └── index.js
│   ├── assets/             # 静态资源
│   ├── components/         # 公共组件
│   │   ├── layout/         # 布局组件
│   │   │   ├── AdminLayout.vue
│   │   │   ├── AdminSidebar.vue
│   │   │   └── AdminHeader.vue
│   │   ├── chart/          # 图表组件
│   │   └── common/         # 通用组件
│   ├── composables/        # 组合式函数
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── stores/             # Pinia 状态管理
│   │   ├── user.js
│   │   └── app.js
│   ├── views/              # 页面视图
│   │   ├── dashboard/      # 仪表盘
│   │   │   └── Index.vue
│   │   ├── article/        # 文章管理
│   │   │   ├── List.vue
│   │   │   └── Edit.vue
│   │   ├── category/       # 分类管理
│   │   ├── tag/            # 标签管理
│   │   ├── comment/        # 评论管理
│   │   ├── link/           # 友链管理
│   │   ├── daily/          # 每日一问
│   │   ├── media/          # 媒体库
│   │   ├── entertainment/  # 娱乐管理
│   │   ├── about/          # 关于我
│   │   └── login/          # 登录页
│   ├── utils/              # 工具函数
│   │   ├── request.js      # Axios 封装
│   │   └── auth.js         # 认证工具
│   ├── App.vue
│   └── main.js
├── index.html
├── package.json
├── vite.config.js
└── element-plus/
```

### 3.2 前台页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│                        移动端头部                                │
│  [头像] Fu Chengyan    [主题] [搜索] [菜单]                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────┴─────────────────────────────────────┐
│  ┌─────────────┐  ┌─────────────────────────────────────────┐   │
│  │             │  │                                         │   │
│  │   侧边栏    │  │              主内容区                    │   │
│  │             │  │                                         │   │
│  │  [头像]     │  │  ┌───────────────────────────────────┐  │   │
│  │  Fu Chengyan│  │  │      每日一问卡片                  │  │   │
│  │             │  │  └───────────────────────────────────┘  │   │
│  │  [社交链接]  │  │                                         │   │
│  │  - Email    │  │  ┌───────────────────────────────────┐  │   │
│  │  - GitHub   │  │  │      文章卡片 1                    │  │   │
│  │  - Twitter  │  │  └───────────────────────────────────┘  │   │
│  │  - Telegram │  │                                         │   │
│  │             │  │  ┌───────────────────────────────────┐  │   │
│  │  [搜索框]   │  │  │      文章卡片 2                    │  │   │
│  │             │  │  └───────────────────────────────────┘  │   │
│  │  [导航菜单]  │  │                                         │   │
│  │  - 主页     │  │  ┌───────────────────────────────────┐  │   │
│  │  - 文章     │  │  │      文章卡片 3                    │  │   │
│  │  - 友链     │  │  └───────────────────────────────────┘  │   │
│  │  - 娱乐     │  │                                         │   │
│  │  - 关于我   │  │  ...更多文章...                          │   │
│  │             │  │                                         │   │
│  │  [底部信息]  │  │                                         │   │
│  │  - 语言切换  │  │                                         │   │
│  │  - 主题切换  │  │                                         │   │
│  │  - 运行时间  │  │                                         │   │
│  └─────────────┘  └─────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 前台页面路由

采用 **Hash 路由** 实现 SPA 效果：

| 路由 | 页面 | 说明 |
|------|------|------|
| `#home` | 首页 | 文章列表 + 每日一问 |
| `#archives` | 文章归档 | 按年份分组展示 |
| `#links` | 友链 | 友情链接列表 |
| `#media` | 娱乐 | 影视/游戏记录 |
| `#about` | 关于我 | 个人介绍 |
| `#post-{slug}` | 文章详情 | 文章内容 + 评论 |

### 3.4 前台数据流

```
┌─────────────┐     Axios     ┌─────────────┐     API      ┌─────────────┐
│   Vue 组件   │ ────────────→ │  Pinia Store│ ────────────→ │  后端 API   │
│             │ ←──────────── │  状态管理    │ ←──────────── │             │
└─────────────┘   响应式更新   └─────────────┘   JSON 响应   └─────────────┘
```

### 3.5 前台交互功能

#### 3.5.1 每日一问

```
┌─────────────────────────────────────────────────┐
│                 每日一问卡片                      │
├─────────────────────────────────────────────────┤
│  [每日一问]              [◀ 前一天] [日期] [后一天 ▶] │
│                                                 │
│  什么是最好的编程语言？                           │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ 没有最好的编程语言，只有最适合的...        │   │
│  │                                         │   │
│  │        👉 点击查看答案                    │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  [❤️ 42]  [💬 8]                                │
└─────────────────────────────────────────────────┘
```

功能点：
- 日期切换：支持前后翻页
- 日历选择：点击日期打开日历
- 答案遮罩：点击显示/隐藏答案
- 点赞功能：本地存储防重复

#### 3.5.2 文章卡片

```
┌─────────────────────────────────────────────────┐
│  [搭建网站]                                      │
│                                                 │
│  我的博客主题已开源，欢迎使用                      │
│                                                 │
│  基于 Hugo Theme Stack 打造的开箱即用博客模板...   │
│                                                 │
│  📅 2026-04-24  ⏱️ 3 分钟  👁️ 252  🌐 中文      │
└─────────────────────────────────────────────────┘
```

功能点：
- 分类筛选：点击分类标签筛选文章
- 悬停效果：卡片上浮 + 阴影
- 响应式布局：移动端单列，桌面端双列

#### 3.5.3 搜索功能

```
┌─────────────────────────────────────────────────┐
│  搜索文章                                   [×]  │
├─────────────────────────────────────────────────┤
│  🔍 输入关键词...                                │
├─────────────────────────────────────────────────┤
│  搜索结果：                                      │
│  - 我的博客主题已开源                              │
│  - 用 Cloudflare Workers 免费给博客增加 AI 助手    │
└─────────────────────────────────────────────────┘
```

### 3.6 后台页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  ┌─────────────┐  ┌─────────────────────────────────────────┐   │
│  │             │  │  [☰] 仪表盘                    [🔔]     │   │
│  │  [头像]     │  ├─────────────────────────────────────────┤   │
│  │  Fu Chengyan│  │                                         │   │
│  │             │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │   │
│  │  [编辑资料]  │  │  │文章  │ │已发布│ │浏览量│ │友链  │   │   │
│  │             │  │  │ 总数 │ │      │ │      │ │      │   │   │
│  │  [社交链接]  │  │  │  6   │ │  5   │ │ 1050 │ │  4   │   │   │
│  │             │  │  └──────┘ └──────┘ └──────┘ └──────┘   │   │
│  │  [导航菜单]  │  │                                         │   │
│  │  - 仪表盘   │  │  ┌─────────────────────────────────┐   │   │
│  │  - 文章     │  │  │  📝 文章管理        →           │   │   │
│  │  - 分类     │  │  ├─────────────────────────────────┤   │   │
│  │  - 标签     │  │  │  🔗 友链管理        →           │   │   │
│  │  - 评论     │  │  ├─────────────────────────────────┤   │   │
│  │  - 友链     │  │  │  🖼️ 媒体库          →           │   │   │
│  │  - 媒体库   │  │  ├─────────────────────────────────┤   │   │
│  │  - 娱乐     │  │  │  📂 分类管理        →           │   │   │
│  │  - 每日一问  │  │  ├─────────────────────────────────┤   │   │
│  │  - 关于我   │  │  │  🏷️ 标签管理        →           │   │   │
│  │             │  │  ├─────────────────────────────────┤   │   │
│  │  [功能区域]  │  │  │  💬 评论管理        →           │   │   │
│  │  - 语言切换  │  │  ├─────────────────────────────────┤   │   │
│  │  - 主题切换  │  │  │  💡 每日一问        →           │   │   │
│  │             │  │  ├─────────────────────────────────┤   │   │
│  │             │  │  │  👤 关于我          →           │   │   │
│  └─────────────┘  │  └─────────────────────────────────┘   │   │
│                    └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.8 后台功能模块

#### 3.8.1 仪表盘

- 文章统计：总数、已发布、草稿、总浏览量
- 快捷入口：文章管理、友链管理、媒体库等

#### 3.8.2 文章管理

- 文章列表：支持搜索、分类筛选、状态筛选
- 文章编辑：Markdown 编辑器 + 实时预览
- 发布设置：分类、标签、封面图、摘要

#### 3.8.3 分类管理

- 分类卡片：显示图标、名称、描述、文章数
- CRUD 操作：新建、编辑、删除分类

#### 3.8.4 标签管理

- 标签云：显示名称、文章数
- CRUD 操作：新建、编辑、删除标签

#### 3.8.5 评论管理

- 评论列表：显示用户、内容、状态
- 审核操作：通过、拒绝、删除

#### 3.8.6 友链管理

- 友链卡片：显示头像、名称、URL
- 审核操作：通过、拒绝

#### 3.8.7 媒体库

- 文件上传：支持拖拽上传
- 文件列表：网格展示，支持搜索

#### 3.8.8 每日一问

- 问题管理：问题、答案、日期、状态
- 评论管理：审核每日一问的评论

#### 3.8.9 关于我

- 个人资料：头像、用户名、简介
- 社交链接：管理社交账号链接

---

## 四、后端设计

### 4.1 目录结构

```
cmd/server/main.go          程序入口
internal/
├── app/app.go              应用启动器
├── api/
│   ├── router.go           总路由
│   └── v1/                 各模块 Controller
├── service/                Service 业务层（接口 + 实现分离）
│   ├── user_interface.go   接口定义
│   ├── user_service.go     实现
│   ├── article_interface.go
│   ├── article_service.go
│   └── ...
├── repository/             Repository 数据访问层（接口 + 实现分离）
│   ├── user_interface.go   接口定义
│   ├── user_repository.go  实现
│   ├── article_interface.go
│   ├── article_repository.go
│   └── ...
├── model/
│   ├── entity/             实体
│   └── dto/
│       ├── request/        请求 DTO
│       └── response/       响应 DTO
└── middleware/              中间件
pkg/
├── config/                 配置
├── database/               数据库
├── logger/                 日志
├── jwt/                    JWT
├── errors/                 错误处理
└── response/               统一响应
configs/                    配置文件
docs/                       文档
```

**文件命名规范**：

| 类型 | 文件名 | 示例 |
|------|--------|------|
| 接口定义 | `{module}_interface.go` | `user_interface.go` |
| 接口实现 | `{module}_service.go` | `user_service.go` |
| 数据访问接口 | `{module}_interface.go` | `user_interface.go` |
| 数据访问实现 | `{module}_repository.go` | `user_repository.go` |

**代码示例**：

```go
// user_interface.go - 接口定义
type UserService interface {
    GetUserByID(id uint) (*entity.User, error)
    GetUserByUsername(username string) (*entity.User, error)
}

// user_service.go - 实现
type userService struct {
    userRepo repository.UserRepository
}

func NewUserService(userRepo repository.UserRepository) UserService {
    return &userService{userRepo: userRepo}
}

func (s *userService) GetUserByID(id uint) (*entity.User, error) {
    return s.userRepo.FindByID(id)
}
```

### 4.2 数据库设计

#### 4.2.1 ER 关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Category  │       │   Article   │       │     Tag     │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │◄──┐   │ id (PK)     │   ┌──►│ id (PK)     │
│ name        │   └───│ category_id │   │   │ name        │
│ slug        │       │ title       │   │   │ slug        │
│ description │       │ slug        │   │   └─────────────┘
│ icon        │       │ content     │   │         ▲
│ sort_order  │       │ summary     │   │         │
└─────────────┘       │ cover       │   │    article_tags
                      │ view_count  │   │    (多对多)
                      │ like_count  │   │
                      │ status      │   │
                      │ is_top      │   │
                      └──────┬──────┘   │
                             │          │
                             │          │
                      ┌──────┴──────┐   │
                      │   Comment   │   │
                      ├─────────────┤   │
                      │ id (PK)     │   │
                      │ content     │   │
                      │ nickname    │   │
                      │ email       │   │
                      │ article_id  │───┘
                      │ parent_id   │───┐ (自关联)
                      │ status      │   │
                      └─────────────┘   │
                                        │
                      ┌─────────────┐   │
                      │    User     │   │
                      ├─────────────┤   │
                      │ id (PK)     │   │
                      │ username    │   │
                      │ password    │   │
                      │ nickname    │   │
                      │ email       │   │
                      │ avatar      │   │
                      └─────────────┘   │
                                        │
                      ┌─────────────┐   │
                      │    Link     │   │
                      ├─────────────┤   │
                      │ id (PK)     │   │
                      │ name        │   │
                      │ url         │   │
                      │ description │   │
                      │ avatar      │   │
                      │ status      │   │
                      └─────────────┘   │
                                        │
                      ┌─────────────────┴─┐
                      │   DailyQuestion   │
                      ├───────────────────┤
                      │ id (PK)           │
                      │ question          │
                      │ answer            │
                      │ date              │
                      │ like_count        │
                      │ comment_count     │
                      └───────────────────┘
```

#### 4.2.2 数据表设计

**users 表（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| username | varchar(50) | 用户名（唯一） |
| password | varchar(255) | 密码（加密） |
| nickname | varchar(50) | 昵称 |
| email | varchar(100) | 邮箱（唯一） |
| avatar | varchar(500) | 头像 URL |
| bio | text | 个人简介 |
| status | tinyint | 状态：1-启用 0-禁用 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**articles 表（文章表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| title | varchar(200) | 标题 |
| slug | varchar(200) | URL 别名（唯一） |
| content | longtext | 内容（Markdown） |
| summary | text | 摘要 |
| cover | varchar(500) | 封面图 URL |
| category_id | uint | 分类 ID（外键） |
| view_count | bigint | 浏览量 |
| like_count | bigint | 点赞数 |
| comment_count | bigint | 评论数 |
| status | varchar(20) | 状态：published/draft |
| is_top | boolean | 是否置顶 |
| reading_time | int | 阅读时长（分钟） |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**article_tags 表（文章标签关联表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| article_id | uint | 文章 ID（联合主键） |
| tag_id | uint | 标签 ID（联合主键） |

**categories 表（分类表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| name | varchar(50) | 分类名称（唯一） |
| slug | varchar(50) | URL 别名（唯一） |
| description | varchar(200) | 描述 |
| icon | varchar(10) | 图标 |
| sort_order | int | 排序 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**tags 表（标签表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| name | varchar(50) | 标签名称（唯一） |
| slug | varchar(50) | URL 别名（唯一） |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**comments 表（评论表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| content | text | 评论内容 |
| nickname | varchar(50) | 昵称 |
| email | varchar(100) | 邮箱 |
| website | varchar(200) | 网站 |
| avatar | varchar(500) | 头像 |
| article_id | uint | 文章 ID（外键） |
| parent_id | uint | 父评论 ID（0 为顶级） |
| status | varchar(20) | 状态：pending/approved/rejected |
| ip | varchar(50) | IP 地址 |
| user_agent | varchar(500) | 浏览器信息 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**links 表（友链表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| name | varchar(100) | 网站名称 |
| url | varchar(500) | 网站 URL |
| description | varchar(200) | 描述 |
| avatar | varchar(500) | 头像 URL |
| logo | varchar(10) | Logo 图标 |
| sort_order | int | 排序 |
| status | varchar(20) | 状态：pending/approved/rejected |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**daily_questions 表（每日一问表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| question | text | 问题 |
| answer | text | 答案 |
| date | varchar(10) | 日期（唯一） |
| like_count | bigint | 点赞数 |
| comment_count | bigint | 评论数 |
| view_count | bigint | 浏览量 |
| status | tinyint | 状态：1-启用 0-禁用 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**media 表（媒体文件表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| filename | varchar(255) | 文件名 |
| url | varchar(500) | 文件 URL |
| size | bigint | 文件大小（字节） |
| mime_type | varchar(100) | MIME 类型 |
| type | varchar(20) | 类型：image/document |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

**entertainment 表（娱乐表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| title | varchar(200) | 标题 |
| title_en | varchar(200) | 英文标题 |
| type | varchar(20) | 类型：movie/tv/game |
| year | int | 年份 |
| cover | varchar(500) | 封面图 URL |
| rating | decimal(3,1) | 我的评分 |
| rating_external | decimal(3,1) | 外部评分 |
| platform | varchar(50) | 平台 |
| playtime | varchar(20) | 游玩时长 |
| comment | text | 评论 |
| status | varchar(20) | 状态：watching/watched/playing/completed |
| link | varchar(500) | 外部链接 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_at | datetime | 删除时间（软删除） |

### 4.3 接口设计

#### 4.3.1 接口规范

- **基础 URL**: `http://localhost:8080/api/v1`
- **数据格式**: JSON
- **认证方式**: JWT Token
- **响应格式**: 统一使用 `pkg/response`

#### 4.3.2 接口列表

详见 [api.yaml](api.yaml) 或 [api.md](api.md)

#### 4.3.3 错误码规范

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 400 | 请求错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 1000+ | 用户/认证业务错误 |
| 2000+ | 参数错误 |
| 3000+ | 资源错误 |

### 4.4 认证设计

#### 4.4.1 JWT 流程

```
┌─────────────┐                    ┌─────────────┐
│   客户端     │                    │   服务端     │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │  POST /auth/login                │
       │  {username, password}            │
       │ ────────────────────────────────→ │
       │                                  │
       │                                  │ 验证用户名密码
       │                                  │ 生成 JWT Token
       │                                  │
       │  {token, expires_at}             │
       │ ←──────────────────────────────── │
       │                                  │
       │  存储 Token (localStorage)        │
       │                                  │
       │  GET /articles                   │
       │  Authorization: Bearer {token}   │
       │ ────────────────────────────────→ │
       │                                  │
       │                                  │ 验证 Token
       │                                  │ 解析用户信息
       │                                  │
       │  {code: 0, data: [...]}          │
       │ ←──────────────────────────────── │
```

#### 4.4.2 Token 存储

- **前台**: `localStorage` 存储
- **后台**: `localStorage` 存储
- **请求头**: `Authorization: Bearer {token}`

---

## 五、部署设计

### 5.1 部署架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         生产环境                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      Cloudflare                         │   │
│  │              CDN 加速 + SSL + DDoS 防护                   │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                       Nginx                              │   │
│  │          静态资源服务 + 反向代理 + 负载均衡                 │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   Go 应用服务                            │   │
│  │              Gin 框架 + GORM                             │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────┬───────────────────────────────┐   │
│  │         MySQL           │          Redis                 │   │
│  │       业务数据           │         缓存数据               │   │
│  └─────────────────────────┴───────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name fcyan.xyz;

    # 前台静态资源（Vue 构建产物）
    location / {
        root /path/to/blog-web/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后台管理（Vue 构建产物）
    location /admin {
        alias /path/to/blog-admin/dist;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
    }

    # API 反向代理
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Swagger 文档
    location /swagger {
        proxy_pass http://127.0.0.1:8080;
    }
}
```

### 5.3 Docker 部署

```dockerfile
# Dockerfile
FROM golang:1.21-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o server ./cmd/server

FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/server .
COPY --from=builder /app/configs ./configs

EXPOSE 8080
CMD ["./server"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - redis
    environment:
      - MYSQL_HOST=mysql
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - REDIS_HOST=redis
      - JWT_SECRET=${JWT_SECRET}

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=blog
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data:
```

---

## 六、开发规范

### 6.1 Git 分支规范

```
main          主分支（生产环境）
├── develop   开发分支
│   ├── feature/xxx   功能分支
│   ├── fix/xxx       修复分支
│   └── refactor/xxx  重构分支
└── release/x.x.x    发布分支
```

### 6.2 提交规范

```
feat:     新功能
fix:      修复 bug
docs:     文档更新
style:    代码格式（不影响功能）
refactor: 重构
perf:     性能优化
test:     测试相关
chore:    构建/工具相关
```

### 6.3 代码规范

- **Go**: 遵循 [Go Code Review Comments](https://github.com/golang/go/wiki/CodeReviewComments)
- **Vue**: 遵循 [Vue 3 风格指南](https://vuejs.org/style-guide/)
- **JavaScript/TypeScript**: 使用 ES6+ 语法，避免 `var`
- **CSS**: 使用 Tailwind CSS 原子化类名，CSS 变量管理主题

### 6.4 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件名 | 小写 + 下划线 | `user_service.go` |
| 包名 | 小写单词 | `service` |
| 接口 | 大写开头 | `UserService` |
| 实现 | 小写开头 | `userService` |
| 构造函数 | `New` + 类型 | `NewUserService` |

---

## 七、性能优化

### 7.1 前端优化

- **懒加载**: 图片使用 `loading="lazy"`
- **代码分割**: 按页面拆分 JS/CSS
- **缓存策略**: 静态资源使用长期缓存
- **压缩**: 开启 Gzip/Brotli 压缩

### 7.2 后端优化

- **数据库索引**: 为常用查询字段添加索引
- **Redis 缓存**: 热点数据缓存（文章列表、阅读量）
- **连接池**: 数据库连接池配置
- **分页查询**: 避免全表扫描

### 7.3 监控指标

- **QPS**: 每秒请求数
- **响应时间**: P50、P95、P99
- **错误率**: 4xx、5xx 错误比例
- **数据库**: 连接数、慢查询

---

## 八、扩展规划

### 8.1 功能扩展

- [ ] RSS 订阅
- [ ] 站点地图
- [ ] 全文搜索（Elasticsearch）
- [ ] 图片懒加载
- [ ] 文章目录自动生成
- [ ] 代码高亮
- [ ] 数学公式支持
- [ ] 多语言支持

### 8.2 技术升级

- [ ] WebSocket 实时通知
- [ ] GraphQL API
- [ ] 微服务拆分
- [ ] Kubernetes 部署

---

## 附录

### A. 参考文档

- [Gin 官方文档](https://gin-gonic.com/docs/)
- [GORM 官方文档](https://gorm.io/docs/)
- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [Element Plus 官方文档](https://element-plus.org/)
- [Tailwind CSS 官方文档](https://tailwindcss.com/)
- [Pinia 官方文档](https://pinia.vuejs.org/)
- [OpenAPI 3.0 规范](https://swagger.io/specification/)
- [JWT 规范](https://jwt.io/introduction)

### B. 相关链接

- 项目仓库: https://github.com/fcy222fcy/blog
- 在线文档: http://localhost:8080/swagger/index.html
- 前台原型: prototypes/web/index.html
- 后台原型: prototypes/admin/index.html

### C. 前端依赖说明

**前台（blog-web）依赖**

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.0",
    "axios": "^1.7.0",
    "marked": "^12.0.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

**后台（blog-admin）依赖**

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.0",
    "axios": "^1.7.0",
    "element-plus": "^2.7.0",
    "@element-plus/icons-vue": "^2.3.0",
    "md-editor-v3": "^4.12.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "unplugin-auto-import": "^0.17.0",
    "unplugin-vue-components": "^0.27.0"
  }
}
```
